# Git Notes

First! Make a .gitignore

## Commands

```
git init
git add <filename>
git commit -m 'Commit Message'
git add -A
git reset --hard
```

## Common Workflows

Reguarly Committing

```
git a -A
git commit -m 'Msg'
```